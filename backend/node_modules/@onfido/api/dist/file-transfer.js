"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.attachFileTransferInterceptor = exports.FileTransfer = void 0;
const fs_1 = require("fs");
const axios_1 = require("axios");
class FileTransfer {
    constructor(data, filename) {
        if (filename == null) {
            this.buffer = (0, fs_1.readFileSync)(data);
            this.filename = data.toString();
        }
        else {
            this.buffer = data;
            this.filename = filename;
        }
    }
}
exports.FileTransfer = FileTransfer;
const instrumented = new WeakSet();
function attachFileTransferInterceptor(axios) {
    if (instrumented.has(axios)) {
        return;
    }
    instrumented.add(axios);
    axios.interceptors.response.use((response) => __awaiter(this, void 0, void 0, function* () {
        if (response.headers instanceof axios_1.AxiosHeaders &&
            response.headers["content-type"]) {
            if (!response.headers["content-type"]
                .toString()
                .includes("application/json")) {
                const contentDisposition = response.headers["content-disposition"];
                var filename = "";
                if (contentDisposition && contentDisposition != "") {
                    const matcher = contentDisposition.match(/filename=['\"]?([^'\"\s]+)['\"]?/);
                    if (matcher != null) {
                        filename = matcher[1].replace(/.*[/\\\\]/g, "");
                    }
                }
                response.data = new FileTransfer(response.data, filename);
            }
        }
        return response;
    }));
}
exports.attachFileTransferInterceptor = attachFileTransferInterceptor;
